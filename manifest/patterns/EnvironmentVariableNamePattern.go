package patterns

const (

	// EnvironmentVariableNamePattern - a pattern for ensuring environment variable identifiers work with the opsys
	EnvironmentVariableNamePattern = `^[a-zA-Z][a-zA-Z0-9_]{0-64}$`
)
